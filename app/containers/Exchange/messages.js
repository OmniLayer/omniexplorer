/*
 * Exchange Messages
 *
 * This contains all the text for the Exchange container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Exchange';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Exchange container!',
  },
});
