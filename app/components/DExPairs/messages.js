/*
 * DExPairs Messages
 *
 * This contains all the text for the DExPairs component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DExPairs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DExPairs component!',
  },
});
