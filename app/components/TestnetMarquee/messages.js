/*
 * TestnetMarquee Messages
 *
 * This contains all the text for the TestnetMarquee component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TestnetMarquee';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TestnetMarquee component!',
  },
});
