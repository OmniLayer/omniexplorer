/*
 * DExOrders Messages
 *
 * This contains all the text for the DExOrders component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DExOrders';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DExOrders component!',
  },
});
