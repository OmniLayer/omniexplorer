/*
 * TransactionType Messages
 *
 * This contains all the text for the TransactionType component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TransactionType';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransactionType component!',
  },
});
