/*
 * DExHistory Messages
 *
 * This contains all the text for the DExHistory component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DExHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DExHistory component!',
  },
});
