import React from 'react';
import PropTypes from 'prop-types';

const ActivationAmount = props => (
  <div>
  </div>
);

ActivationAmount.propTypes = {
  purchases: PropTypes.any.isRequired,
};

export default ActivationAmount;
