/*
 * TransactionAmount Messages
 *
 * This contains all the text for the TransactionAmount component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.TransactionAmount.header',
    defaultMessage: 'This is the TransactionAmount component !',
  },
});
