/*
 * ClassABTransaction Messages
 *
 * This contains all the text for the ClassABTransaction component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ClassABTransaction';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClassABTransaction component!',
  },
});
